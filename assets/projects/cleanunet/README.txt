Drop two audio files here to activate the before/after player on the
CleanUNet page:

  noisy_sample.wav     (the noisy input)
  denoised_sample.wav  (the model output)

MP3 also works — if you use .mp3, rename them noisy_sample.mp3 / denoised_sample.mp3
and tell me, and I'll switch the file extensions in the page.
